version: "3.1"

rules:
- rule: ask for crop disease at start
  steps:
    - intent: start
    - action: utter_greet

- rule: Fallback rule
  steps:
  - intent: nlu_fallback
  - action: action_custom_fallback


